# Write your MySQL query statement below
SELECT query_name , round(avg(rating/position),2) as quality , round(sum(rating<3)/count(rating)*100,2) AS poor_query_percentage FROM Queries GROUP BY query_name ORDER BY query_name DESC;